Титульный
Актуальность
Цель и задачи

Реализация.
	От гит-хука нам небоходим только сигнал о том, что модуль на сервере обновился. Бальше всю работу делает сервер.


	Манифест модуля должен находиться в той же директории, что и файл спецификации пакета CocoaPods .podspec
	Манифест модуля должен быть только один.
	Файл спецификации модуля, который будет использоваться при сборке должен носить имя пакета CocoaPods (имя дирректории + ".podspec")

	При анализе интерфейса используются только переменные с правами доступа {get set}
	При большем количестве зависимостей чем переменных, используются толкьо те зависимости, для которых есть переменные.

	В интрефейсе модуля могут быть описаны переменные !,?,[],[]?
	Переменная описанная с знаком ? является слабой зависимостью по определению.
	Переменная описанная без знака или со знаком ! является сильной зависимостью по определению
	Любое несоответствие ведет к ошибке
	В манифесте приложения указывается, какие модули должны быть помещены в каждый массив